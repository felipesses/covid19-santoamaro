import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:covid19_santoamaro/app/modules/stats/stats_page.dart';

main() {
  testWidgets('StatsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(StatsPage(title: 'Stats')));
    //  final titleFinder = find.text('Stats');
    //  expect(titleFinder, findsOneWidget);
  });
}
