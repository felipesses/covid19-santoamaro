import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:covid19_santoamaro/app/modules/stats/stats_controller.dart';
import 'package:covid19_santoamaro/app/modules/stats/stats_module.dart';

void main() {
  initModule(StatsModule());
  // StatsController stats;
  //
  setUp(() {
    //     stats = StatsModule.to.get<StatsController>();
  });

  group('StatsController Test', () {
    //   test("First Test", () {
    //     expect(stats, isInstanceOf<StatsController>());
    //   });

    //   test("Set Value", () {
    //     expect(stats.value, equals(0));
    //     stats.increment();
    //     expect(stats.value, equals(1));
    //   });
  });
}
